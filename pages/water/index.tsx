import type { NextPage } from "next";
import { useRef } from "react";
import Canvas from "@/components/Canvas";

const Home: NextPage = () => {
  // return <Canvas />;
  return <>준비중</>;
};

export default Home;
